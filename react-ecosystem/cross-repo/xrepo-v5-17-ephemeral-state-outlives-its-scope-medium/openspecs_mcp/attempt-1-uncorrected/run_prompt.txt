You are performing a cross-repository information-retrieval task over the
React state-management ecosystem.

CASE:       xrepo-v5-17-ephemeral-state-outlives-its-scope
ECOSYSTEM:  14 sibling repositories. They are separate products with separate
            release trains; most of them do not import one another, and there is
            no monorepo, workspace or shared package graph tying them together.
INDEX:      openspecs-index knowledge graph (MCP)
            These repositories are NOT checked out on this machine and no copy of
            them is readable from the filesystem. They reach you only as indexed
            knowledge, at exactly the commit below and at no other revision. The
            repositories at those commits are the whole world for this task: do
            not use any other revision, and do not rely on knowledge of these
            projects' later history.

  repo             commit
  redux            3aa561f9fc13f3287e6d83764f85fe0e82437c5f
  redux-toolkit    b1c5130154c454ca8387f55da6122fcb507c560c
  react-redux      ad5d1e0816d0cb1464b25cf2853f2a7d73433f5b
  reselect         8d87c27b75f55883629be75d1eae1c27832d907a
  redux-thunk      184205d49f707c6f203269e0d39ad85824801816
  react            3a717e42438afac81020cdec297dadb5613a4304
  jotai            5c4ca26b0db5571114be58393e17854a771f7790
  zustand          beca84e600e4e250f6b244d22878e72948f331c7
  db               7f0fa36ff6d48b0494ed5f6a1223fd18c317e41f
  xyflow           360f5b13e2bc6899ea06b4be1a49b068d86926cf
  query            46d7f02f1c7b9fcd3255082cc7103e8bfa3dab76
  table            d08af367e11c539bb7e18c2472aa761149ef6db6
  tldraw           5590d14d8edd4faab7dc1177b6e1adb28876fd23
  redux-devtools   f4b4668c30ae08920c59a76cc4629c35c16ef0fa

PROBLEM
A behaviour-level description of a defect and the contract it belongs to. The
identifiers used in the code are deliberately withheld, so searching for the
words below will not find the answer:

Transient visual feedback in one of these libraries outlives the thing it belongs to: the fading trail left behind by a presentation pointer keeps animating and drawing after the user has switched to a different page of the same document, because the component that owns those in-flight trails is one long-lived manager that never recorded which page each trail was started on, and nothing tears them down when the active page changes. The same shape of bug is a standing contract across the folder: short-lived state - the transient status of an in-flight fetch, a pending selection, an interaction still in progress - is owned by something that outlives the scope the state actually belongs to, and has to be reset when that scope changes underneath it. Which files own scope-bound ephemeral state of this kind and would have to change?

TASK
Identify every file, in every repository listed above, that would have to change
to satisfy the described contract. The answer spans MULTIPLE repositories that do
not import one another - do not stop at the first repository that matches, and do
not assume the repository where the symptom appears is the only one affected.

RULES
- Use the openspecs-index MCP server, and nothing else. That is your whole
  surface: no filesystem - no Bash, Read, Grep or Glob, and no checkout to run
  them against - no WebSearch or WebFetch, no subagents. Every path you name
  must have come back from an mcp__openspecs-index__* call. A run that reaches
  the answer by another route is a void measurement, not a faster one.
- Open with roll_call to get each repository's knowledgeId. A knowledgeId is an
  opaque string: take it from roll_call, never guess or infer it. Omit
  knowledgeId on stakeout / manhunt / dragnet / cross_repo_lookup and the call
  sweeps every indexed repository at once - looping the roster one repo at a
  time is the fastest way to waste this run.
- The graph's prose fields (purpose, summary, businessContext) were written by
  an LLM at index time and are leads, not evidence. The structural fields -
  relativePath, qualifiedName, startLine/endLine, commitHash, call edges - come
  from a parser. Rank on structure, and confirm behaviour with the_receipts
  before you assert it.
- Do not stop at the first plausible file, and do not stop at the first repository.
  Work outward from the symptom to the contract: once you can state the rule the
  defect breaks, find every other file in the roster that defines, enforces,
  relies on, or violates that same rule.
- Return repo-relative paths: no leading slash, no repository name prefix.
  Example: packages/<package-name>/src/<SomeFile>.ts
- Every path must exist in that repository AT THE PINNED COMMIT. Pass the pinned
  commitHash on every call that accepts one, and name only paths that came back
  carrying that commitHash. Drop anything you cannot anchor to it - you have no
  filesystem to fall back on, so an unanchored path is a guess.
- Name source files only. Ignore tests, docs, examples, fixtures, benchmarks,
  build output, dependency directories and generated API reports.
- Prefer the files that define, enforce, or violate the contract over files that
  merely mention it.
- Rank most likely first and cap the whole answer at 40 paths across all
  repositories combined.

RUN CONDITIONS (MANDATORY - this run is invalid if any of these is not met):
- LAUNCH: one fresh session per run. Prompt caching WITHIN this run is fine -
  cache this session creates and reads back is expected and costs nothing in
  comparability. What is forbidden is inheriting cache from an EARLIER session:
  do not resume or continue a prior session, do not reuse a session across cases
  or arms, and do not re-attempt a case in a session that already attempted it.
  A run that starts warm on someone else's context is not a measurement.
- INDEX COVERAGE: before searching, confirm the graph is actually live for THESE
  repositories at THESE commits. roll_call reports only each repository's NEWEST
  indexed commit, so a row showing a different commit does NOT mean the pinned
  one is absent - probe it by passing the pinned commitHash to a search before
  you conclude anything. A repository may also be absent from the graph
  altogether. Report plainly, in your reasoning, any roster repository you could
  not reach at the pinned commit, and answer only from what the graph actually
  returned: do not fill a gap from parametric memory, from another revision, or
  with a guessed list.
- COST: cost.json must record cache_read and cache_creation next to the USD
  figure, and both must be attributable to THIS run alone. If the session was
  resumed, reused, or had already attempted the case, set comparable:false with
  a not_comparable_reason in result.json instead of comparing it to clean runs.


DELIVERABLES: the harness captures this run for you - it records the full transcript,
every tool call, and the cost, and it writes ranked.json from your final message. You
have no write tools; do not attempt to create files. Your only job is the ranked answer.

FINAL MESSAGE
Output ONLY a JSON array, most likely first, with no prose and no markdown fence.
One object per repository you are naming; omit repositories you are not naming:
[{"repo": "<repo>", "files": ["<path>", "..."]}, ...]
